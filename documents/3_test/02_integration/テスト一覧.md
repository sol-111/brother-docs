# 結合テスト一覧

## 概要

項目,内容
プロジェクト,BROTHER
テスト種別,結合テスト
作成日,2026-04-09
更新日,2026-06-09

## テスト方針

- 結合テストでは「画面をまたぐデータ連携」「外部API連携」「E2Eフロー」を確認する
- 単画面内で完結するバリデーション・UI制御は単体テスト（01_unit/機能一覧）でカバー済みのため、結合テストには含めない

## テストシナリオ一覧

### エンドユーザー系

テストID,シナリオ名,主な結合点,担当者,完了
IT-01,新規会員登録〜原稿一覧表示,新規登録 → メール認証 → 企業情報保存 → 初期行事自動作成 → 原稿一覧遷移,,
IT-02,ログイン〜画面遷移分岐,認証API → ロール / 登録状態による遷移先分岐 → 全保護画面アクセス制御,,
IT-03,パスワード再設定フロー,PW再設定メール送信 → リンク遷移 → PW更新 → 新PWでログイン,,
IT-04,アカウント情報変更,PW照合 → PW更新 / メアド認証メール → 認証リンク → メアド更新 → 新認証情報でログイン,,
IT-05,企業情報登録〜行事情報初期化,企業情報保存 → 初期行事自動作成 → 原稿一覧反映,,
IT-06,行事新規作成〜原稿生成〜ダウンロード,行事保存 → 評価 → POST /generated-images → ポーリング → GET /files/{fileKey}/url → DL,,
IT-07,行事編集〜原稿再生成,行事更新/複製/削除 → 再生成API → 画面反映,,
IT-08,共通印刷物ダウンロード,業界別グループ表示制御 → 印刷物一覧 → GET /files/{fileKey}/url → DL,,
IT-09,ノベルティ閲覧,サイドメニュー → 業界小分類に紐づくPDF別タブ表示,,
IT-10,年度切り替え〜一覧表示更新,サイドメニュー年度変更 → 各一覧再取得 → ロール別メニュー表示 → 各画面遷移,,

### 管理者系

テストID,シナリオ名,主な結合点,担当者,完了
IT-11,業界・行事テンプレート CRUD,業界CRUD → テンプレートCRUD → エンドユーザー側反映,,
IT-12,デザインテンプレート CRUD,GET /source-image-groups → サイズ別画像取得 → テンプレCRUD → 行事テンプレート側反映,,
IT-13,評価ログ CSV ダウンロード,年度/月絞り込み → JavaScript CSV生成（DLログ/評価ログ） → DL,,
IT-14,共通印刷物管理 CRUD,グループCRUD（業界紐づけ） → GET /common-print-groups → エンドユーザー側反映,,

## テスト観点

観点,説明
画面遷移の連鎖,操作の結果として正しい画面へ遷移すること
画面間のデータ連携,保存したデータが次画面・次回アクセス時に正しく表示されること
外部API連携,Brother APIのリクエスト・レスポンスが正しく処理されること
ロール / 認証状態による制御,認証状態・ロールに応じてアクセス制御が正しく機能すること
管理者→エンドユーザー反映,管理者が作成・更新したデータがエンドユーザー側に正しく反映されること
